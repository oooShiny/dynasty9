{#
/**
 * @file
 * Thin bridge → dynasty_tw:main-menu SDC.
 *
 * Available variables:
 *   - items: Nested menu item array from theme_get_function() / menu_build_tree().
 *   - attributes: HTML attributes object for the top-level <ul> (not used — classes
 *     are hardcoded in the SDC to match the Patriots brand style).
 */
#}
{% include 'dynasty_tw:main-menu' with {
  items: items,
} only %}
