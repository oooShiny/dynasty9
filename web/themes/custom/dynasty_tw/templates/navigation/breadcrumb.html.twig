{#
/**
 * @file
 * Thin bridge → dynasty_tw:breadcrumb SDC.
 *
 * Available variables:
 *   - breadcrumb: Array of {url, text} objects from template_preprocess_breadcrumb().
 */
#}
{% include 'dynasty_tw:breadcrumb' with {
  breadcrumb: breadcrumb,
} only %}
