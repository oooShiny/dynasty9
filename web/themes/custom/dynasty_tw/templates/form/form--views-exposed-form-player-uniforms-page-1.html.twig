{#
/**
 * @file
 * Thin bridge → dynasty_tw:exposed-form SDC.
 * Player uniforms page — centered auto-width form with Patriots white background.
 */
#}
{% set rendered_attrs %}{{ attributes.addClass('flex mx-auto p-4 patriots-white views-exposed-form w-fit mt-5') }}{% endset %}
{% include 'dynasty_tw:exposed-form' with {
  rendered_attrs: rendered_attrs,
  form_children:  children,
} only %}
