{#
/**
 * @file
 * Thin bridge → dynasty_tw:highlight-media-only SDC.
 *
 * Passes Drupal Attribute objects through so the SDC can apply
 * any module-injected classes/attributes (e.g. contextual links).
 */
#}
{% include 'dynasty_tw:highlight-media-only' with {
  url:                url,
  label:              label,
  attributes:         attributes,
  content_attributes: content_attributes,
  title_attributes:   title_attributes,
  video:              content.field_muse_video_id,
} only %}
