{#
/**
 * @file
 * Thin bridge → dynasty_tw:exposed-form SDC.
 * Game search page — column layout with dividers.
 */
#}
{% set rendered_attrs %}{{ attributes.addClass('divide-y divide-gray-500 flex flex-col gap-2') }}{% endset %}
{% include 'dynasty_tw:exposed-form' with {
  rendered_attrs: rendered_attrs,
  form_children:  children,
} only %}
