{#
/**
 * @file
 * Thin bridge → dynasty_tw:facets-sidebar SDC.
 *
 * Available variables:
 *   - facets:     Array of facet objects — each with 'title' (string) and
 *                 'content' (render array).
 *   - show_title: Boolean — whether to render facet title headings.
 */
#}
{% include 'dynasty_tw:facets-sidebar' with {
  show_title: show_title ? true : false,
  facets:     facets,
} only %}
