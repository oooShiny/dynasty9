{#
/**
 * @file
 * Thin bridge → dynasty_tw:exposed-form SDC.
 * Highlight search page — horizontal flex layout.
 */
#}
{% set rendered_attrs %}{{ attributes.addClass('flex gap-2') }}{% endset %}
{% include 'dynasty_tw:exposed-form' with {
  rendered_attrs: rendered_attrs,
  form_children:  children,
} only %}
