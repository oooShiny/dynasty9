name: Main Menu
description: Primary site navigation — a flex horizontal list with uppercase font-light labels and recursive sub-menu support.
group: Navigation

props:
  type: object
  properties:
    items:
      type: array
      title: Menu items
      description: "Nested Drupal menu items. Each item has: title (string), url (Drupal\\Core\\Url), attributes (Attribute object), below (child items array), is_expanded, is_collapsed, in_active_trail."
