{# Using iframe method to avoid CORS issues with skiv.com API #}
{% for item in items %}
  {% set muse_id = item.content['#context']['value'] %}

  <iframe src="https://skiv.com/embed/{{ muse_id }}"
          style="width: 100%; aspect-ratio: 16/9;"
          allowfullscreen
          allow="autoplay; fullscreen"
          loading="lazy"></iframe>
{% endfor %}
