<div>
  {% for section, section_links in admin_links %}
    <div>
      <h2>{{ section|title }}</h2>
      <ul>
        {% for link, title in section_links %}
          <li><a href="{{ link }}">{{ title }}</a></li>
        {% endfor %}
      </ul>
    </div>
  {% endfor %}
</div>
